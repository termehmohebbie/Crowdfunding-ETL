CREATE TABLE contact (
  contact_id int not null,
  first_name varchar(15),
  last_name varchar(15),
  email varchar(40),
  PRIMARY KEY (contact_id)
  );
 INSERT INTO contact (contact_id,first_name, last_name, email)
 values (1,'termeh','mohebbie','termeh@nlm.com');
 SELECT * FROM contact;
 
 
 CREATE TABLE categories (
  category_id varchar not null,
  category varchar(15),
  PRIMARY KEY (category_id)
  );
 INSERT INTO categories (category_id,category)
 values ('cat1','food');
 SELECT * FROM categories;
 
 CREATE TABLE subcategories (
  subcategory_id varchar not null,
  subcategory varchar(15),
  PRIMARY KEY (subcategory_id)
  );
 INSERT INTO subcategories (subcategory_id,subcategory)
 values ('scat1','lebanese');
 SELECT * FROM subcategories;
 
 CREATE TABLE campaign (
   cf_id int not null,
   contact_id int,
   company_name varchar,
   description varchar,
   goal float,
   pledged float,
   outcome varchar(15),
   backers_count int,
   country varchar(4),
   currency varchar(6),
   launch_date date,
   end_date date,
   category_id varchar,
   subcategory_id varchar,
   PRIMARY KEY (cf_id),
   FOREIGN KEY (contact_id) REFERENCES contact(contact_id),
   FOREIGN KEY (category_id) REFERENCES categories(category_id),
   FOREIGN KEY (subcategory_id) REFERENCES subcategories(subcategory_id)
 );
 INSERT INTO campaign (cf_id, contact_id, company_name, description, goal, pledged, outcome, backers_count, country, currency, launch_date, end_date, category_id,subcategory_id)
 values (147,1,'NLM','DEALERSHIP',1500,1865.25,'successful',5,'CA','CAD','2020-02-03','2022-08-11','cat1','scat1');
 SELECT * FROM campaign;